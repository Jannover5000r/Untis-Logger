package Untis

import (
	"log"
)

func Run() {
	log.Println("Run")
	type res struct {
		Name string
		url  string
		key  int
	}
	/*	r := res{"Jannik", "google.com", 4}
			r, err := json.Marshal(r)
		if err != nil {
			log.Printf("Error marshalling JSON: %v", err)
			return
		} */
}
